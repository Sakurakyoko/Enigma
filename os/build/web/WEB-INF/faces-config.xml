<?xml version='1.0' encoding='UTF-8'?>
<faces-config version="2.0"
              xmlns="http://java.sun.com/xml/ns/javaee"
              xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
              xsi:schemaLocation="http://java.sun.com/xml/ns/javaee http://java.sun.com/xml/ns/javaee/web-facesconfig_2_0.xsd">
  <application>

    <!-- The locale-config tag is used to specify the available languages.
    The default-locale specifies the language to use if the user's preferred
    language is not known, or if none of the preferred languages are
    supported. The supported-locale tag is used to specify addiontal
    languages that the application supports. The language that will be
    used is determined by the preferred language settings in the browser. -->
    <locale-config>
      <default-locale>en</default-locale>
      <supported-locale>no</supported-locale>
    </locale-config>

    <!-- The resource-bundle tag specifies where the language-specific
    text can be found. The filenames for the language files are constructed
    by appending one of the locale identifiers from the local-config tag,
    and a .properties extension to the base-name (e.g. uistrings_en.properties).
    The file can be found in the java package specified in the base-name.
    The variable name strings can be used in the facelets to refer to the
    language-spesific text -->
    <resource-bundle>
      <base-name>is202.hrms.web.uistrings</base-name>
      <var>strings</var>
    </resource-bundle>
  </application>

</faces-config>
